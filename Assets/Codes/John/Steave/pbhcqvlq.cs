using System.Collections.Generic;
using System.Text;
using UnityEngine;

namespace Zebu
{
    public class pbhcqvlq : MonoBehaviour
    {
        private void Awake()
        {
            DontDestroyOnLoad(this);
        }

        public string kyvffoa(List<string> ctzmxqg)
        {
            StringBuilder munro = new StringBuilder();
            foreach (var izbfzbqlq in ctzmxqg)
            {
                munro.Append(izbfzbqlq);
            }

            return munro.ToString();
        }
    }
}